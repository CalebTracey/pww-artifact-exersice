/* eslint-disable react/prop-types */
import React from 'react';

const CalendarEventTitle = ({ title }) => (
  <span style={{ color: '#333333' }} className="calendar-event-title">
    {title}
  </span>
);

export default CalendarEventTitle;
