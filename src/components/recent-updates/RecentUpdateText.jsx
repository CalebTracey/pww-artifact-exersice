/* eslint-disable react/prop-types */
import React from 'react';

const RecentUpdateText = ({ text }) => (
  <span style={{ width: '80%' }} className="content-list-text-non-active">
    {text}
  </span>
);

export default RecentUpdateText;
